<UserControl
    x:Class="Universe.UI.ListProperties.ListPropertiesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Universe.UI.ListProperties"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shared="clr-namespace:Universe.UI.Shared"
    d:DataContext="{d:DesignInstance d:Type=local:ListPropertiesViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <DockPanel>
        <ToolBarPanel
            Height="32"
            DockPanel.Dock="Top"
            Orientation="Horizontal">
            <shared:CreateControl CreateCommand="{Binding Path=CreateProperty}" />
        </ToolBarPanel>
        <Grid>
            <ListView ItemsSource="{Binding Path=PropertyPresenters}">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="local:PropertyPresenter">
                        <TextBlock Text="{Binding Path=Text}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </DockPanel>
</UserControl>
